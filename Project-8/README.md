state management = sharing state + maintaing state

context store implementation

1) Create context object
2) place values to context store
3) provide the store to parent component( all children get access)
4) now compoennets can access the state from store